import { Toolbar, AppBar } from '@material-ui/core';

function headerChatBox() {
    return (
        <AppBar>
            <Toolbar>
              
            </Toolbar>            
        </AppBar>
    );
 }

export default headerChatBox;
